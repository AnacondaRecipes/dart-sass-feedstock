anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: sass/dart-sass
      use_max_tag: true
      include_regex: '^\d+\.\d+\.\d+$'
      from_pattern: '^(\d+\.\d+\.\d+)$'
      to_pattern: '\1'
    packages:
      - dart-sass
